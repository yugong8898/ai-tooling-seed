---
alwaysApply: true
---

# <项目名> 通用规范（Cursor 规则）

## 语言规则
- 回复使用中文
- 必要时会明确要求英文
- **称呼探针（强制）**：每次回复必须使用称呼「<结尾称呼探针，如 wang 工>」，置于回复**末尾**、紧邻【规则对齐】标识之前一行。该称呼是「rules 已加载」的探针——用户重置上下文后，若回复末尾无该称呼，即说明 rules 未生效，须重新加载后再回复。（Cursor 端语言/称呼规则实际落在本文件；`core-project-rules.mdc` 仅作指针索引，不内联规则。）

## 综合开发规范
- 提交遵循 Conventional Commits
- 组件用函数式 + Hooks / `<script setup>`
- 样式 kebab-case，CSS 属性顺序参考规范
