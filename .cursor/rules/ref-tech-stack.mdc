---
description: {{PROJECT_NAME}} 技术栈索引；处理代码前按目标路径读取对应 stack 文档
alwaysApply: false
---

# 技术栈索引

项目识别结果见 `.cursor/ai-tooling.json`，人工确认后的详细规范见 `.cursor/prompts/stack-index.md`。

- 依赖版本以各包 `package.json` 和锁文件为准。
- 样式方案以真实 `.css`、`.less`、`.sass`、`.scss` 文件为准，不跨包套用。
- 启动、构建、测试和 lint 命令以各包 scripts 为准。
- CDN、接口、金额工具、浏览器兼容和版本禁区必须由代码或项目文档证明。
