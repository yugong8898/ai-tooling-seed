---
name: bugfix-helper
description: 复现、定位和修复线上或测试缺陷；用户要求排查报错或 bug 时使用
---

# Bugfix Helper

1. 收集复现步骤、预期/实际、环境、日志、频率和最近变更。
2. 读取对应技术栈与相关代码，建立按可能性排序的假设。
3. 用日志、断点、最小用例或 `git log/blame` 验证假设，不凭直觉定根因。
4. 根因确认后先写失败回归测试，再做最小修复。
5. 运行相关测试和受影响路径回归，报告命令与结果。

输出包含根因证据、影响范围、修复文件、回归测试和剩余风险。
