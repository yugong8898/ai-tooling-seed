# 项目技术栈文档体系搭建与维护手册

> 用途：任何新项目启动时，按本文档快速生成一套「精准、单源、跨 AI 工具通用」的 AI 协作脚手架——
> 涵盖技术栈文档、项目知识库、可复用技能、代码片段、项目 README 与各 AI 工具的项目级配置。
> 设计来源：采用「一个索引 + 每类独立详述」结构，并镜像到 `.codebuddy` / `.qoder` / `.cursor` 三工具目录。本手册为**自包含标准**，不依赖任何外部项目目录（wlyd 等仅作可选本地示例，见第十二章）。
> 整理人：王新骏　最后更新：2026年07月

---

## 〇、如何使用本文档（必读）

- **本文档是「规范」，不是「产出」。** 正文（第一～十章）是从零搭建的**正向强制约束**：照着做，第一步就正确。
- **若你正在维护某个已落地项目（当前是 `yd/`）**：凡是改动本文档里涉及 `README.md` / `AGENTS.md` / `stack-*` / `role-*` / `rules` / `skills` / `snippets` / `context` 的条款，**必须同轮把改动真正落到该项目对应文件**，改完确认"已更新 `yd/xxx`"，而不是只说"模板已更新"。
- **第十一章是「规定来由」（历史踩坑清单）**，仅解释"为什么这么规定"，可作为背景阅读，但不读也能照正文做对。
- **正确路径**：新项目 → 直接按 **第五章生成流程** 一气呵成；老项目维护 → 按 **第六章维护约定**。

---

## 一、核心原则（正向强制约束）

以下原则已把历史踩坑的正确做法固化为前置约束，违背任何一条都会重蹈覆辙：

1. **一个索引 + 每类独立文件**：技术栈不内联进角色 prompt，避免散落失真；所有技术栈事实只写一处（`prompts/stack-*`）。
2. **唯一可编辑源在 `.md/prompts/`**：技术栈与角色 prompt 的唯一源放在 `<proj>/.md/prompts/`（`stack-index` + `stack-*` + 11 个 `role-*`）；技术栈文件（`stack-index.md` / `stack-*.md`）**只存在于 `.md/prompts/` 单源**，**不镜像进 `rules/`**。改技术栈只改 `.md/prompts/` 这一处。
3. **`rules/` 只放一个项目规则文件**：`.codebuddy/rules/` 与 `.qoder/rules/` 各只放**一个** `<proj>-project-rules.md`（`alwaysApply: true` frontmatter，承载综合开发规范 + 技术栈索引指针），**不放 `stack-*` 镜像**。Cursor 用 `.cursor/rules/*.mdc` 模块化规则（带 frontmatter，按 `core-/tech-/ref-` 分类），**无根 `.cursorrules` 单文件**。三处 `project-rules` 内容一致，不擅自精简任一份；技术栈索引改 `.md/prompts/` 后须同步进 `.cursor/rules/ref-tech-stack.mdc` 与 role 引用。
4. **以事实为准，禁止脑补**：先读各子项目 `package.json` 与样式文件（`*.less` / `*.scss` / `*.css`）核实；**不假设 Sass/SCSS**——若全仓不用须显式声明「不使用 Sass/SCSS」，若并存须逐项目标明。其他项目的**专属内容**（CDN 域名、数字格式化、账号隔离等）一律不照搬，须以当前项目实际代码为准。
5. **框架/版本逐项目标**：不笼统写「React 18 + TS + Less 多端」，各子项目版本可能不同；版本禁区（如 react-dom 16.x 勿升级、Umi2 老栈）逐项目写清。
6. **技能与片段可复用**：通用流程（review / commit / workflow / 记忆更新）沉淀为 skill；高频代码范式沉淀为 snippet。
7. **`README.md` 与 `AGENTS.md` 必产且达参考密度**：两份都是必产项，不可只产其一。子项目表须含真实技术栈、含仓库结构树；`AGENTS.md` 须含 Skills / Snippets / Prompts 角色 / Context 四张索引表（格式标杆见第四章 AGENTS 模板）。
8. **Cursor 用 `.cursor/rules/*.mdc` 模块化规则**：规则放 `.cursor/rules/` 目录（带 frontmatter，按 `core-/tech-/ref-` 分类），**不建根目录 `.cursorrules` 单文件**。`core-*` 类 `alwaysApply: true` 每次对话必加载，`tech-*` 类按 `globs` 文件匹配，`ref-*` 类按 `description` 智能加载；`.cursor/` 目录同时放 `skills/` `snippets/` `context/` `settings.json` `extensions.json`。（wlyd / bondee / yd 均统一采用此模块化形态。）
9. **知识库无 `.md/context` 统一源**：`api-patterns` / `business-terms` / `known-pitfalls` **直接维护在各工具目录**（yd：`.codebuddy/context/`、`.cursor/context/`；Bondee 另放 `.qoder/` 根，yd 不采用）。**不建 `.md/context/` 层、不镜像三处**；内容须为 `prompts/stack-*` 之外的真实项目知识，**不重复技术栈事实、不留空壳文件**。
10. **skills/snippets 三处（或两处）镜像勿漏**：`.codebuddy/skills/` 为权威源 → 同步 `.qoder/skills/` 与 `.cursor/skills/`（Cursor 确有独立 `skills/` 与 `snippets/` 目录，见第二章目录结构）；snippets 同步到 `.cursor/snippets/`。每个 SKILL.md 内的路径与引用名须与实际目录对齐。
11. **模板变更须落地项目文件**：本文件任何改动若会让在维护项目（yd）的产出文件与之不一致，必须同轮落地并确认（见第十一章·来由 9）。
12. **规则变更必须三工具同轮同步（防漂移铁律）**：`.codebuddy/rules/<proj>-project-rules.md` 为单一事实源（SSOT），`.qoder/rules/<proj>-project-rules.md` 与其字节一致；`.cursor/rules/core-project-rules.mdc` 为 Cursor 端入口索引（短指针，含 `alwaysApply: true` frontmatter），AI 协作与独立判断规则单独成 `.cursor/rules/core-ai-collaboration.mdc`（正文须与 SSOT AI 协作章节字节一致）。任一端改动，必须**同一轮**落地三端；严禁只改一处。提交前必须跑 `ai-tooling-seed/scripts/verify-rules-sync.sh <项目根>` 校验通过，否则视为未同步完成（可选接 git pre-commit hook 自动拦截）。各项目 `AGENTS.md` 也须声明此约束，不能只靠本文档。

> **规则变更 SOP（每次改规则走 5 步）**
> 1. 改 SSOT：`.codebuddy/rules/<proj>-project-rules.md`
> 2. 同步 `.qoder/rules/<proj>-project-rules.md`（内容字节一致，可直接 `cp`）
> 3. 同步 `.cursor/rules/core-project-rules.mdc`（短索引指针，含 `alwaysApply: true` frontmatter），并同步 `.cursor/rules/core-ai-collaboration.mdc`（AI 协作章节，正文与 SSOT 一致）
> 4. 跑 `bash ai-tooling-seed/scripts/verify-rules-sync.sh <项目根>`
> 5. 校验通过后再提交
>
> 三工具文件集允许按技术栈不同存在 `tech-*` / `ref-*` 差异，但 `core-project-rules.mdc` + `core-general-standards.mdc` 这套 `core-*` 基线必须每个项目都齐备。
>
> **AI 协作规则独立成 `core-ai-collaboration.mdc`**：AI 协作与独立判断是独立主题，在 Cursor 模块化体系里须为独立的 `core-ai-collaboration.mdc`（`alwaysApply: true`，正文与 SSOT 对应章节字节一致）；`core-project-rules.mdc` 仅作 Cursor 端入口索引（短指针），不再内联 AI 协作章节，避免与 SSOT 形成冗余副本漂移。

---

## 二、完整目录结构（脚手架全貌）

```
<proj>/
├── .md/                                  # ★ 唯一可编辑源（技术栈 + 角色 prompt）
│   └── prompts/                          # 技术栈 + 角色 prompt（源）
│       ├── stack-index.md                # 索引表：子项目 → stack 文件 → 特殊规范
│       ├── stack-<type1>.md ...          # 每类技术栈独立详述
│       └── role-*.md ...                 # 11 个角色 prompt（role-pm/architect/coder/reviewer/qa-checklist/bugdetective/handoff/refactor/resume/ui-reviewer/workflow）
├── .codebuddy/                           # 镜像副本（CodeBuddy）
│   ├── rules/                            # 仅 <proj>-project-rules.md（alwaysApply 综合规范 + 技术栈索引指针）
│   ├── context/                          # 知识库（★直接维护，非镜像自 .md/context）
│   ├── skills/                           # 技能（<name>/SKILL.md，frontmatter）— 权威源
│   └── snippets/                         # 代码片段模板（.md）
├── .qoder/                               # 镜像副本（Qoder）
│   ├── rules/                            # 同 .codebuddy/rules/
│   └── skills/                           # 镜像 .codebuddy/skills/
├── .cursor/                              # Cursor 配置目录
│   ├── rules/                            # 模块化规则 *.mdc（core-/tech-/ref- 分类，带 frontmatter；无根 .cursorrules 单文件）
│   ├── skills/                           # 技能（镜像 .codebuddy/skills/）
│   ├── snippets/                         # 代码片段（镜像 .codebuddy/snippets/）
│   ├── context/                          # 知识库（直接维护，非镜像自 .md/context）
│   ├── settings.json                     # 编辑器设置
│   └── extensions.json                   # 推荐扩展
├── AGENTS.md                             # AI 协作指南（子项目表 + 四类索引，必产）
└── README.md                             # 项目总览（人类 + AI 共读，必产）
```

> `<type>` 命名示例（按构建/样式/框架分组）：`react-umi`、`react-vite`、`react-webpack-ssr`、`react-figma`、`vue3-vite`、`vue2-admin` 等。

---

## 三、各 AI 工具目录对照表

| 工件 | `.codebuddy` | `.qoder` | `.cursor` | 单源位置 |
|------|--------------|----------|-----------|----------|
| 技术栈索引/详述 | （不进 rules/，见下「项目规则指针」） | （不进 rules/） | `.cursor/rules/ref-tech-stack.mdc`（模块化，指针） | `.md/prompts/`（唯一源：`stack-index.md` + `stack-*.md`） |
| 项目知识库 | `context/*.md`（按需） | （默认不建；Bondee 放根 `*.md`，yd 不采用） | `context/*.md`（按需） | 各工具目录直接维护（**无 `.md/context` 统一源**） |
| 技能 skills | `skills/<name>/SKILL.md` | `skills/<name>/SKILL.md` | `skills/<name>/SKILL.md`（镜像） | `.codebuddy/skills/` |
| 代码片段 | `snippets/*.md` | — | `snippets/*.md`（镜像） | `.codebuddy/snippets/` |
| 项目规则指针 | `rules/<proj>-project-rules.md` | 同左 | `.cursor/rules/core-project-rules.mdc`（模块化，指针） | — |
| 编辑器配置 | — | — | `settings.json` + `extensions.json` | `.cursor/` |
| 项目总览 | — | — | — | `README.md`（根，单源） |
| AI 协作指南 | — | — | — | `AGENTS.md`（根，单源） |

> 技术栈事实**只在 `.md/prompts/` 一处**（不进 `rules/`）；Cursor 的 `.cursor/rules/ref-tech-stack.mdc` 持一份技术栈索引指针，改动须同步。知识库 `context/*.md` 直接在各工具目录维护，不强制三处一致、也无统一源。

---

## 四、文件模板（直接复制填充，替换 `<...>`）

> ⚠️ **关于「副本：唯一可编辑源为 …」注释**：技术栈 `stack-*` 文件**只存在于 `.md/prompts/` 单源**，不复制到 `rules/` 或 `.cursor/rules/`（历史上误把 `stack-*` 塞进 `rules/` 且成空壳，已纠正）；`.cursor/rules/ref-tech-stack.mdc` 的技术栈索引段可加「技术栈唯一源：`.md/prompts/stack-index.md`」注释以便溯源。**项目规则文件 `<proj>-project-rules.md` 本身即规范源**（带 `alwaysApply: true`），不在 `rules/` 放任何 stack 副本。

### 4.1 `stack-index.md`（索引，只做表）

```markdown
# <项目名> 项目技术栈索引（stack-index）

根据文件路径归属的子项目，找到对应技术栈文件，再引用该文件获取完整规范。

> ⚠️ **重要**：本仓库<统一使用 Less / 同时使用 Sass 与 Less，按子项目而定，切勿跨项目混用 / 不使用 Sass/SCSS>。<按实际填写禁止项>

## 项目 → 技术栈文件

| 子项目 | 技术栈文件 | UI 库 | 样式方案 | 特殊规范 |
|--------|-----------|-------|---------|---------|
| `<sub-project-A/>` | [stack-<type1>.md](stack-<type1>.md) | <antd/element/...> | <Less/Sass/CSS> | <如：React 17 勿升级；SSR 禁顶层 window> |
| `<sub-project-B/>` | [stack-<type2>.md](stack-<type2>.md) | ... | ... | ... |

---

**文档版本**: v1.0
**最后更新**: <YYYY年MM月>
**整理人**: <姓名>
```

### 4.2 `stack-<type>.md`（每类技术栈独立详述，固定小节）

```markdown
# 技术栈：<框架 + 构建>（<子项目定位>）

## 适用项目
- `<sub-project-A/>`：<说明（构建 + 框架版本 + UI 库）>

## 技术栈
- **框架**：<React/Vue X> + TypeScript
- **构建**：<Vite/Umi/Webpack/Koa>
- **UI 库**：<antd/element/...>
- **状态管理**：<Redux/Zustand/Pinia/...>
- **路由**：<react-router/ vue-router/Umi/...>
- **多语言**：<i18next/...>
- **样式**：<Less（CSS Modules）/ Sass / 原生 CSS>

## 样式规范（重要）
- 类名 <kebab-case / ...>；<Less 嵌套与 DOM 对应，禁止平铺>
- <如：禁止 flex `gap`（iOS 14 塌陷），用 `margin` 替代>
- CSS 属性顺序遵循团队规范（布局 → 盒模型 → 排版 → 视觉 → 其他）

## 代码规范要点
### 函数组件 + Hooks
\`\`\`tsx
// 给出该项目约定写法的代码片段
\`\`\`
### <状态管理 / 路由 / 多语言 / 数字格式化 / 资源 CDN> 等要点
- <项目特有的关键约定，且以实际代码为准>

## 不支持的特性
- <无 Sass / SCSS（只使用 Less/CSS）>
- <无 Vue / Options API / 其他不适用项>
- <项目停留在某版本时的禁区，如「react-dom 16.x 勿升级」>

## <验证 / 构建> 命令
\`\`\`bash
cd <sub-project> && npx <eslint/lint/build> <path>
\`\`\`
```

> 特殊项目增删小节：Figma 插件加「主/UI 线程 postMessage 通信、原生 CSS」；SSR 加「模块顶层禁 window/document、客户端逻辑放 useEffect」；Vue 项目把「函数组件」换成「`<script setup>` 组件」。

### 4.3 Cursor `.cursor/rules/*.mdc`（模块化规则，带 frontmatter）

> Cursor 规则放在 `.cursor/rules/` 目录下的 `.mdc` 文件（带 `---` frontmatter，按 `core-/tech-/ref-` 分类控制加载）。**本项目（yd / bondee / wlyd）统一采用「`.cursor/rules/*.mdc` 模块化」方案，不建根 `.cursorrules` 单文件。** 技术栈索引、版本禁区、项目特定规则拆到对应 `.mdc`（`ref-tech-stack.mdc` / `core-project-rules.mdc` 等），完整详述仍指向 `.md/prompts/stack-*.md`。

```markdown
# <proj> 编码规则

## 1. 代码提交规则
- 无明确命令时不做任何代码提交操作
- 只有用户明确要求时才生成 commit 信息

## 2. 语言规则
- 回复使用中文
- **称呼探针（强制）**：每次回复末尾须含称呼「<结尾称呼探针，如 wang 工>」（紧邻【规则对齐】之前一行），作为「rules 已加载」的探针。Cursor 端此规则实际落在 `core-general-standards.mdc`；`core-project-rules.mdc` 仅作指针索引。

## 3. 技术栈索引（强制前置）
<内联 stack-index 表格 + 版本禁区 + Sass/Less 分用警告>

## 4. 项目特定规则
- 组件引用 / API 调用 / 国际化（vue-i18n / i18next）/ 状态管理
- 项目特有工具（CDN、格式化等）以实际代码为准，详见 `.md/prompts/stack-index.md`

## 使用说明
此文件被 Cursor 自动读取；完整技术栈见 `.md/prompts/`。
```

### 4.4 `project-rules.md` 末尾指针段

```markdown
---

## 技术栈索引（强制前置）

修改任何子项目代码前，**先根据本仓库技术栈索引确定其技术栈与样式规范**：

- 技术栈索引（唯一源）：`.md/prompts/stack-index.md`（按文件路径归属子项目查对应技术栈文件）
- 各子项目详述：`.md/prompts/stack-<type>.md`（同上，不在本 `rules/` 目录）
- ⚠️ <本仓库不使用 Sass/SCSS / 同时使用 Sass 与 Less，按子项目而定>

> 本文件（`<proj>-project-rules.md`）是项目综合开发规范源（带 `alwaysApply: true`），技术栈详细事实见 `.md/prompts/stack-*`，不在此重复；如需同步技术栈索引，改 `.md/prompts/` 后同步更新 `.cursor/rules/ref-tech-stack.mdc` 指针段。
```

（Cursor 无独立根 `.cursorrules` 单文件：上述「技术栈索引（强制前置）」段落在 `.cursor/rules/ref-tech-stack.mdc` 指针文件中。）

### 4.5 知识库 `context/*.md`（直接维护在各工具目录，按需）

> **没有 `.md/context/` 统一源**。`api-patterns` / `business-terms` / `known-pitfalls` **直接写到各工具目录**，按需创建、不必三处一致。yd 实际只落在 `.codebuddy/context/` 与 `.cursor/context/`（默认空目录，有真实内容才填）；Bondee 额外放 `.qoder/` 根，但 yd **不采用**。
>
> 🚫 **铁律：知识库不得重复 `prompts/stack-*` 已有的技术栈事实**（框架、样式、版本禁区、请求封装位置、i18n 方案全在 stack 里）。知识库只放 stack 之外的**真实项目知识**：真实 API 约定、已核实的业务术语/职责、从实际踩坑提炼的非栈坑点。

**`api-patterns.md`** —— API 调用模式（仅 stack 未覆盖的真实约定）
```markdown
# <项目> API 调用模式
> 技术栈层面的请求封装见 `.md/prompts/stack-*`；本文件只记本项目特有约定。
## 一、项目特有约定
- <如：统一错误码处理、分页约定、鉴权 header 注入点>
## 二、典型示例
\`\`\`ts
// 给出本项目真实 API 写法
\`\`\`
```

**`business-terms.md`** —— 业务术语 / 子项目职责（职责栏是重点，技术栈列只指向 stack）
```markdown
# <项目> 业务术语与子项目职责
| 子项目 | 职责（以实际为准） | 技术栈文件 |
|--------|------|--------|
| `<sub-A/>` | <做什么，未核实标「待补」> | [stack-<type>.md](...) |
\`\`\`
```

**`known-pitfalls.md`** —— 已知坑点（编码前对照，只记 stack 之外的真实坑）
```markdown
# <项目> 已知坑点速查
> 从实际踩坑中提炼，编码前主动对照。技术栈版禁区（react-dom 16.x、Umi2、Sass/Less 混用、SSR 禁 window）已在 `prompts/stack-*`，不在此重复。
## 一、业务/逻辑坑
- <如：某接口幂等、某状态机非法跃迁>
```

### 4.6 技能 `skills/<name>/SKILL.md`

```markdown
---
name: <skill-name>            # 通用技能不加 <proj>- 前缀；仅项目专属技能加（如 <proj>-review）
description: <一句话；何时触发，如「用户说'帮我 review'时使用」>
disable-model-invocation: true   # 可选：禁止模型自动调用，仅用户显式触发
---

# <技能名>

## 激活指令
\`\`\`
@<skill-name> <用法>           # 通用不加前缀；项目专属用 @<proj>-<skill-name>
\`\`\`

## 执行步骤
### 第一步：...
### 第二步：...
```

> **技能命名前缀规则（本手册基准）**：通用技能（`bugfix-helper` / `code-analyzer` / `commit-helper` / `memory-update`）**不加 `<proj>-` 前缀**；仅**项目专属技能**才加前缀（如 `<proj>-review` / `<proj>-workflow` / `<proj>-requirement-change-analysis`）。所有技能在 `.codebuddy/skills/`（权威源）、`.qoder/skills/`（镜像）、`.cursor/skills/`（镜像）三处内容一致。⚠️ **各项目实际命名以自身文件为准，勿凭空套用本基准**：基准将 `code-analyzer`/`commit-helper` 视为通用（不加前缀），但 yd 全部 7 项均不加前缀、bondee 则对 `code-analyzer`/`commit-helper`/`review`/`workflow`/`requirement-change-analysis` 统一加 `bondee-` 前缀、仅 `bugfix-helper`/`memory-update` 不加——属各项目自身约定（详见第八/十章）。代码片段同步到 `.codebuddy/snippets/` 与 `.cursor/snippets/`，qoder 不放 snippets。SKILL.md 内所有路径与引用名须与当前项目实际目录对齐（Cursor 用 `.cursor/rules/*.mdc`，不写根 `.cursorrules` 单文件）。

### 4.7 代码片段 `snippets/*.md`

```markdown
# <组件/方法> 模板（<项目> 规范）
带 JSDoc 头部的标准结构，适用于 <子项目>。
## 使用方式
复制下方模板，替换 `[占位符]`。
## 模板
\`\`\`tsx
// 项目约定的组件 / API 方法范式
\`\`\`
```

### 4.8 `README.md`（仓库根，单源，人类 + AI 共读）

> 质量标杆参考：第四章 README 模板。要求：子项目表带真实技术栈、含仓库结构树、快速开始、开发规范摘要、三工具配置维护说明、相关链接——**不要只放一张空表**。

```markdown
# <项目名>

<项目一句话定位>。采用 **monorepo** 方式组织多个子项目，同时使用 Cursor、Qoder、CodeBuddy 协同维护。

## 项目结构

\`\`\`
<proj>/
├── <sub-A>/              # <职责>（<框架+样式>）
├── .md/                  # 技术栈 + 角色 prompt（唯一可编辑源）
│   └── prompts/
├── .codebuddy/           # CodeBuddy 配置（rules/skills/context/snippets）
├── .qoder/               # Qoder 配置（rules/skills）
├── .cursor/rules/        # Cursor 主配置（模块化 *.mdc）
├── .cursor/              # Cursor 配置（skills/snippets/context/settings）
├── AGENTS.md             # AI 协作指南
└── README.md             # 本文档
\`\`\`

## 子项目

| 子项目 | 说明 | 技术栈 | 入口 |
|--------|------|--------|------|
| `<sub-A/>` | <职责> | <React X · TypeScript · Less（antd · Umi）> | [查看详情](./<sub-A>/README.md) |
| `<sub-B/>` | <职责> | <Vue3 · TypeScript · Sass（Element Plus）> | - |

## 快速开始

### 环境要求
- Node.js >= <版本>
- npm >= <版本>

### 安装与运行
各子项目为独立工程，进入对应目录后按各自 `package.json` 启动：

\`\`\`bash
# 以 <sub-A> 为例
cd <sub-A>
npm install
npm run dev
\`\`\`

## 开发规范

- 文档、注释、Commit 信息统一使用**中文**。
- 无明确命令时不做任何代码提交操作；只在用户明确要求时才生成 commit 信息。
- 修改已有内容时优先**注释保留**原逻辑，不直接删除。
- 技术栈与样式禁区见 `AGENTS.md` 及 `.md/prompts/stack-index.md`；编码前对照索引与对应 `stack-*.md`。

## 配置维护说明

本项目以 Cursor 为主进行配置维护，其他工具同步：
- Cursor 主配置：`.cursor/rules/*.mdc` + `.cursor/`（skills / context / snippets）
- Qoder 同步：`.qoder/rules/` + `.qoder/skills/`
- CodeBuddy 同步：`.codebuddy/`（rules / skills / context / snippets）
- 修改主配置时以 Cursor 侧为准，并同步更新其他工具配置。

## 相关链接
- [AI 协作指南](./AGENTS.md)
- [技术栈索引](./.md/prompts/stack-index.md)
- 知识库（按需）：[已知坑点](./.codebuddy/context/known-pitfalls.md) / [业务术语](./.codebuddy/context/business-terms.md) / [API 模式](./.codebuddy/context/api-patterns.md)

---

**最后更新**: <YYYY年MM月>
**维护人**: <姓名>
```

### 4.9 `AGENTS.md`（AI 协作指南，仓库根，必产）

> 质量标杆参考：第四章 AGENTS 模板。要求：含项目概览、子项目表（技术栈摘要 + 指向 stack-index）、仓库结构树、开发规范摘要、**Skills / Snippets / Prompts 角色 / Context 四张索引表**、三工具同步规则——这是 AI 启动时的主要导航页，信息必须齐全。

```markdown
# <项目名> 项目开发协作指南

## 项目概览

<项目名> 是一个 <领域> 项目，采用 **monorepo** 方式组织多个子项目，同时使用 Cursor、Qoder、CodeBuddy 协同维护。

## 子项目

> 各子项目技术栈细节见 `.md/prompts/stack-index.md`（索引表），其按子项目指向对应 `stack-*.md` 详述文件。**本仓库<统一使用 Less / 同时使用 Sass 与 Less，按子项目而定，切勿跨项目混用 / 不使用 Sass/SCSS>**。
>
> 项目规则在 `.codebuddy/rules/<proj>-project-rules.md`、`.qoder/rules/<proj>-project-rules.md`、`.cursor/rules/core-project-rules.mdc`（指针）三处一致生效；技术栈详述**只在 `.md/prompts/` 单源**（不进 `rules/`），改技术栈后同步 `.cursor/rules/ref-tech-stack.mdc` 指针即可。

| 子项目 | 说明 | 技术栈（摘要） |
|--------|------|----------------|
| `<sub-A/>` | <职责> | <React X · TypeScript · Less（antd · Umi 4）> |
| `<sub-B/>` | <职责> | <Vue3 · TypeScript · Sass（Element Plus · Vite）> |

## 仓库结构

\`\`\`
<proj>/
├── <sub-A>/                # <职责>
├── .md/                    # 技术栈 + 角色 prompt（唯一可编辑源）
│   └── prompts/            # stack-index + stack-* + role-*
├── .codebuddy/             # rules / skills / context / snippets
├── .qoder/                 # rules / skills
├── .cursor/rules/          # Cursor 主配置（模块化 *.mdc）
├── .cursor/                # skills / snippets / context / settings
└── AGENTS.md               # 本文档
\`\`\`

## 开发规范

### 语言与沟通
- 文档与沟通统一使用**中文**；代码注释使用中文；Git commit 信息使用中文。

### 双称呼探针（AGENTS / rules 加载校验，铁律）
- AGENTS 末尾须含「AI 执行纪律标识」段（见 `templates/AGENTS.md`），要求每次回复**开头以「<开头称呼探针，如 honey 王>」起句**（证明 AGENTS 已加载）、**末尾含「<结尾称呼探针，如 wang 工>」称呼**（证明 rules 已加载），并在末尾输出 `【规则对齐】AGENTS✓ rules✓ 横向搜索✓ 三端同步✓ 完成自检✅/⚠️`。
- 复制骨架后须做两件事：① 把 `<开头称呼探针>`/`<结尾称呼探针>` 替换为**两个不同**的实际称呼；② 让 `<结尾称呼探针>` 在三端 rules（`.codebuddy/` `.qoder/` `.cursor/rules/core-general-standards.mdc`）与本 AGENTS 中**逐字一致**。用户重置上下文后：两称呼齐全=双边生效；缺开头=AGENTS 未生效；缺结尾=rules 未生效。

### 代码提交规范
- 无明确命令时不做任何代码提交操作；只在用户明确要求时才生成 commit 信息。
- Commit 格式详见 `.cursor/rules/core-general-standards.mdc` 相关节。

### 技术栈规范（摘要）
- <按 `stack-*.md` 摘录：框架、函数组件+Hooks / `<script setup>`、样式 kebab-case、CSS 属性顺序等>
- ⚠️ <版本禁区，如 react-dom 16.x 勿升级 / Umi2 禁升 / SSR 顶层禁 window / Sass 与 Less 按子项目分用勿混>

## 快速开始

各子项目为相互独立的工程，进入对应目录后按各自 `package.json` 的脚本启动。

\`\`\`bash
# 以 <sub-A> 为例
cd <sub-A>
npm install
npm run dev
\`\`\`

## Skills 索引

> **技能命名前缀规则（本手册基准）**：通用技能（`bugfix-helper` / `code-analyzer` / `commit-helper` / `memory-update`）**不加 `<proj>-` 前缀**；仅**项目专属技能**加前缀（如 `<proj>-review` / `<proj>-workflow` / `<proj>-requirement-change-analysis`）。yd、bondee 实际命名见实战章节（以实际文件为准，勿凭空套用本基准）。下方列出全部 **7 个**技能，与「Prompts 角色索引」保持同步。

| Skill | 说明 | 触发场景 |
|-------|------|---------|
| `workflow` | 全链路研发流程编排器 | "走流程"、"继续上次进度" |
| `review` | 代码审查 | "帮我 review"、"检查改动" |
| `commit-helper` | Commit 信息生成 | "生成提交信息"、"帮我提交" |
| `code-analyzer` | 代码分析器（三种模式） | "分析影响范围"、"读懂这个模块" |
| `requirement-change-analysis` | 需求/变更分析 | "分析需求"、"评估改动影响" |
| `bugfix-helper` | 线上问题排查 | "排查这个问题"、"线上有个 bug" |
| `memory-update` | 记忆整理器 | "整理 memory"、"更新规则" |

> ⚠️ 此表须与「Prompts 角色索引」同步：凡角色引用了某 Skill，此处必须列出；凡本表增删某 Skill，须同步更新另一侧索引表（如 `role-pm` ↔ `requirement-change-analysis`、`role-bugdetective` ↔ `bugfix-helper`）。注意：`commit-helper` / `code-analyzer` / `memory-update` 等 Skill 仅以 Skill 形式存在、无对应角色 prompt，属正常，不必强求角色表一一对应。

## Snippets 模板

| 模板 | 说明 |
|------|------|
| `react-component-template` | React + TypeScript 函数组件模板 |
| `vue-component-template` | Vue 3 `<script setup>` 组件模板 |
| `api-method-template` | <项目> API 接口封装模板 |
| `number-formatter-template` | 数字格式化模板（按需） |

> 模板按项目实际框架增删：Vue 项目须补 `vue-component-template`；无数字格式化需求可省略 `number-formatter-template`。

## Prompts 角色索引（`.md/prompts/`）

手动多 Chat 场景使用，直接 `@.md/prompts/role-xxx.md` 激活。与对应 Skill 编排逻辑一致，修改一处需同步另一处。

**技术栈唯一索引**：`.md/prompts/stack-index.md`。所有角色 prompt 内联技术栈均已改为引用它，修改技术栈只需改这一处。

| 角色文件 | 说明 | 对应 Skill | 触发场景 |
|---------|------|-----------|---------|
| `role-workflow.md` | 全链路研发流程编排器（Prompt 版） | `workflow` | `@role-workflow.md 走流程` |
| `role-pm.md` | 需求分析师 | `requirement-change-analysis` | `@role-pm.md 分析需求` |
| `role-architect.md` | 架构师（技术方案设计） | - | `@role-architect.md 出方案` |
| `role-coder.md` | 开发工程师（逐文件编码） | - | `@role-coder.md 改 xxx.tsx` |
| `role-reviewer.md` | 代码审查员 | `review` | `@role-reviewer.md 审查改动` |
| `role-qa-checklist.md` | 提测清单生成器 | - | `@role-qa-checklist.md 提测` |
| `role-bugdetective.md` | Bug 侦探（根因定位） | `bugfix-helper` | `@role-bugdetective.md 排查 bug` |
| `role-ui-reviewer.md` | UI 还原审查员 | - | `@role-ui-reviewer.md 对比设计稿` |
| `role-refactor.md` | 重构师 | - | `@role-refactor.md 分析 xxx.tsx` |
| `role-resume.md` | 恢复续接员 | - | `@role-resume.md 恢复现场` |
| `role-handoff.md` | 中断交接员 | - | `@role-handoff.md 生成交接文档` |

## 上下文参考文档（Context，按需）

位于 `.codebuddy/context/`（Cursor 同步至 `.cursor/context/`），编码前可 `@` 引用快速获取背景知识：

| 文件 | 说明 |
|------|------|
| `known-pitfalls.md` | 已知坑点汇总（stack 之外的真实坑），编码前检索 |
| `business-terms.md` | 业务术语表与子项目职责 |
| `api-patterns.md` | 接口调用约定（stack 未覆盖的项目特有约定） |

## 配置维护说明

本项目配置文件同步规则：
- **主配置**：`.cursor/rules/*.mdc`（Cursor 读取）+ `.cursor/`
- **同步配置**：
  - `.codebuddy/rules/` - CodeBuddy 规则（与 `.md/prompts/` 同步）
  - `.codebuddy/skills/` - CodeBuddy 技能（活跃 Skill 列表，权威源）
  - `.codebuddy/snippets/` - CodeBuddy 代码片段
  - `.codebuddy/context/` - 业务上下文（按需）
  - `.qoder/rules/`、`.qoder/skills/` - Qoder 同步
- 修改主配置时以 Cursor 侧为准，并同步更新其他工具配置；新增/下线 Skill、Snippet、角色或 Context 文件时，须同步更新本页对应索引表。

---

**文档版本**: v1.0
**最后更新**: <YYYY年MM月>
**整理人**: <姓名>
```

### 4.10 Cursor `settings.json` / `extensions.json`

- `settings.json`：统一 `tabSize:2`、Prettier（`singleQuote`、`semi`、`printWidth:100`）、ESLint 校验、`files.eol:"\n"` 等。
- `extensions.json`：推荐 `esbenp.prettier-vscode`、`dbaeumer.vscode-eslint`、`EditorConfig`；**按项目框架增删**——Vue 项目加 `vue.volar`/`vue.volar-ext`，React 项目加 `dsznajder.es7-react-js-snippets`；不需要的写进 `unwantedRecommendations`。

---

## 五、生成流程（从零开始正确搭建，一步到位）

> 照本流程执行，第一步就正确，无需事后纠错。**在维护 yd 时，本流程产出的每个文件须同时写入 `yd/` 对应路径**（见第十一章·来由 9）。

0. **前置认知**
   - 本规范是"规范"不是"产出"；**若在维护 yd，每完成一步就立即把产物写入 yd/ 对应路径**（不要最后统一补），改完确认"已更新 yd/xxx"（见第十一章·来由 9、原则 11）。
   - Cursor 形态固定：`.cursor/rules/*.mdc` 模块化 + `.cursor/{skills,snippets,context,settings,extensions}`，**不建根 `.cursorrules` 单文件**。（wlyd / bondee / yd 均已迁移至 `.cursor/rules/*.mdc` 模块化形态；新项目默认用本形态，不要混用。）
   - 知识库无 `.md/context` 统一源，直接写各工具目录。

1. **确认 Cursor 形式**：确认采用「`.cursor/rules/*.mdc` 模块化 + `.cursor/` 多目录（skills/snippets/context/settings/extensions）」形态（见第二章目录结构），避免误建根 `.cursorrules` 单文件。如本地已有同类项目可作参考，亦可对照其结构，但标准以本手册为准。

2. **核实真实技术栈**：读各子项目 `package.json` + 搜 `*.less`/`*.scss`/`*.css`，确认框架版本、UI 库、构建工具、样式方案。**不假设 Sass/SCSS**；逐项目标明样式方案与版本禁区。

3. **一次性产出 `.md/prompts/`（唯一源）**：`stack-index.md` + 各 `stack-<type>.md` + **11 个 `role-*.md`** 同批产出，不可只建 stack 漏 role。11 个角色固定为：`role-pm` / `role-architect` / `role-coder` / `role-reviewer` / `role-qa-checklist` / `role-bugdetective` / `role-handoff` / `role-refactor` / `role-resume` / `role-ui-reviewer` / `role-workflow`（见第二章/第十一章）。
   - 通用骨架可复用；**特有事实（CDN 域名、数字格式化、账号隔离、版本禁区）必须以当前项目实际代码为准，不照搬参考项目专属内容**。
   - **角色 prompt 内联的技术栈一律改为引用 `stack-index.md`（不内联）**，确保技术栈只在 `.md/prompts/` 一处（原则 1），改技术栈只改这一处。

4. **建项目规则文件 + 内联技术栈（不往 rules/ 塞 stack）**：在 `.codebuddy/rules/`、`.qoder/rules/` 各建**一个** `<proj>-project-rules.md`（`alwaysApply: true`，载综合开发规范 + 技术栈索引指针，技术栈详述指向 `.md/prompts/`），**不复制 `stack-*` 进 `rules/`**；**Cursor 用 `.cursor/rules/*.mdc` 模块化**（拆 `core-project-rules.mdc` 指针 + `ref-tech-stack.mdc` 技术栈索引，**不建根 `.cursorrules` 单文件**）。技术栈详述只维护在 `.md/prompts/`。

5. **技能与片段（镜像勿漏）**：在 `.codebuddy/skills/` 建技能（`<name>/SKILL.md`，权威源）→ **同步 `.qoder/skills/` 与 `.cursor/skills/`**；在 `.codebuddy/snippets/` 沉淀范式 → **同步 `.cursor/snippets/`**。
   - 通用技能标准 7 项见 4.9（索引表）：`workflow` / `review` / `commit-helper` / `code-analyzer` / `requirement-change-analysis` / `bugfix-helper` / `memory-update`；其中部分技能（如 `code-analyzer`）按项目实际取舍，新项目不必全建。
   - 同时按 4.10 创建 `.cursor/settings.json` + `extensions.json`（按框架增删推荐扩展）。
   - 通读每个 SKILL.md 内的路径与引用名，与实际目录对齐（Cursor 用 `.cursor/rules/*.mdc`，不写根 `.cursorrules` 单文件）。

6. **知识库（按需直接写工具目录）**：直接写 `.codebuddy/context/` 与 `.cursor/context/`（yd 不落 `.qoder/`）。**不建 `.md/context/` 源、不镜像三处**；内容须为 stack 之外的真实知识，不重复技术栈事实、不留空文件。

7. **必产 `README.md` + `AGENTS.md`（两份都达参考密度）**：
   - `README.md`：结构树 + 子项目表（真实技术栈）+ 环境要求与快速开始 + 规范摘要 + 三工具配置维护说明 + 相关链接。
   - `AGENTS.md`：子项目表（指向 stack-index）+ 结构树 + 规范摘要 + **Skills / Snippets / Prompts / Context 四张索引表** + 三工具同步规则。
   - 两份都只**引用**索引、不内联技术栈细节；均含 `文档版本 / 最后更新 / 整理人` 落款。

8. **收尾自检（防遗漏）**：
   - 目录齐全：`.md/prompts/` 含 11 个 role；`.cursor/` 含 `skills/`+`snippets/`+`context/`+`settings.json`+`extensions.json`，**无 `rules/`**；**无 `.md/context/`**。
   - 无 0 字节文件；无空壳 context。
   - 各 SKILL.md 路径引用与实际目录一致。
   - `AGENTS.md` 四张索引表与 `skills/` `snippets/` `prompts/` `context/` 实际内容一致。

---

## 六、维护约定（单源原则，正向）

- **技术栈文档**：只改 `.md/prompts/`（唯一源，不进 `rules/`）；Cursor 索引指针同步到 `.cursor/rules/ref-tech-stack.mdc`。`rules/` 只维护 `<proj>-project-rules.md`（综合规范），不存放 `stack-*`。
- **知识库文档（`context/*.md`）**：无 `.md/context` 统一源，直接在各工具目录（`.codebuddy/context/`、`.cursor/context/`；Bondee 另放 `.qoder/` 根，yd 不采用）维护；内容须为 `prompts/stack-*` 之外的真实项目知识，不重复技术栈事实、不留空文件。
- **技能**：`.codebuddy/skills/` 为权威源，改后同步 `.qoder/skills/` 与 `.cursor/skills/`。
- **`README.md` / `AGENTS.md`**：只引用索引，不内联技术栈细节；新增/下线 Skill、Snippet、角色或 Context 文件时，须同步更新 `AGENTS.md` 对应索引表（Skills / Snippets / Prompts / Context 四表），避免导航页与实际情况脱节。
- **模板变更须同步落地项目文件**：本文件任何改动若会让在维护项目（yd）的 `README.md` / `AGENTS.md` / `prompts` / `rules` / `skills` / `snippets` / `context` 与之不一致，必须同轮把改动真正落到 yd 对应文件并确认。
- 每份副本顶部注释标明源位置，避免改错地方。
- **路径/重命名变更必须全局校验（防漏）**：任何子项目路径调整（整体迁移目录、改名）不能只改「项目归属描述行」，须同步以下所有位置并**逐副本核验**：
  1. 子项目表 / 树形结构图的路径单元格与层级（如 `| siren/siren-h5/`、结构树新增 `siren/` 父层）。
  2. **可执行命令里的路径**：`cd <proj>`、`npm --prefix <proj>`、脚本进入目录等——这是真路径，漏改会导致命令失败（本次事故即漏改 `cd siren-h5` 类启动命令）。
  3. 正文反引号路径（`` `siren/siren-h5/` ``）、各 `stack-*.md` 的归属行与示例路径。
  4. 跨文件引用：角色 prompt（如 `role-architect` 的 SSR 示例路径）、`AGENTS.md` / `README.md` 的快速开始。
  - ⚠️ **全仓搜索会被 gitignore 跳过隐藏目录**（`.codebuddy/`、`.qoder/`、`.cursor/`、`\.cursorrules` 若残留），一次全仓搜索"看似无残留"不可靠；须对 `.md/prompts/`、`.codebuddy/rules/`、`.qoder/rules/`、`.cursor/rules/`、`AGENTS.md`、`README.md` **分别再搜一次**确认。
  - 区分「路径引用」（须带完整相对仓库根前缀，如 `siren/siren-h5`）与「子项目代号」（如 `siren-h5`，作身份标识可保留），勿误改代号。

---

## 七、子项目增删 / 目录调整 文档同步 SOP（可直接照做）

> 适用场景：① 新增子项目；② 删除/下线子项目；③ 子项目改名；④ 子项目整体迁移目录（如从根迁到 `website/`、从根迁到 `siren/`）。
> 核心心法：**技术栈事实只在 `.md/prompts/` 一处改，再逐镜像目录同步；路径引用带完整仓库根前缀，代号保留；改完逐目录复搜确认 0 残留。**
> 本 SOP 由 bondee `website/` 调整（hybrid 迁入 + 2 新项目）与 yd `siren/` 迁移两次实战抽象而成。

### 0. 先判类型，决定改哪些 stack 文件

| 场景 | 判定动作 |
|------|---------|
| 新增子项目 | 读其 `package.json` + 样式文件，确认框架/UI 库/构建/样式方案，**归入已有 `stack-<type>.md` 或新建一个**（禁止脑补，以事实为准） |
| 删除子项目 | 找出它当前归属的 `stack-<type>.md`，确认无其他子项目共用后，从索引与 stack 文件里移除其行 |
| 改名 / 迁移目录 | 确定「新路径前缀」（如 `website/`）与「是否仅代号变 / 路径也变」 |

### 1. 改唯一源 `.md/prompts/`（先做，且只改这里）

- **`stack-index.md`（索引表）**：
  - 新增 → 加一行 `| \`<新路径>/<项目>/\` | [stack-xxx.md] | UI库 | 样式 | 特殊规范 |`；
  - 删除 → 删对应行；
  - 迁移/改名 → 路径单元格加前缀或改全名（如 `spatio-clotho-hybrid/` → `website/spatio-clotho-hybrid/`）。
- **对应 `stack-<type>.md`**：
  - 「## 适用项目」小节：加/改/删项目条目（路径带新前缀）；
  - 「## ESLint 验证命令」等**可执行 `cd` 命令**：路径必须带完整前缀（如 `cd website/spatio-clotho-hybrid && npx ...`）——**这是真路径，漏改命令会失败**，是历史头号坑；
  - 正文反引号示例路径同步改。

### 2. 同步三工具镜像（`.codebuddy/`、`.qoder/`、`.cursor/`）

- `rules/` 只放一个 `<proj>-project-rules.md`，**不存放 `stack-*` 镜像**（技术栈只在 `.md/prompts/`）。三处 `project-rules.md`（`.codebuddy` / `.qoder`）内容须一致；`.cursor/rules/ref-tech-stack.mdc` 技术栈索引指针须与 `.md/prompts/stack-index.md` 对齐。
- ⚠️ **不要往 `rules/` 塞 `stack-*`**：历史事故常因把 `stack-*` 误塞进 `rules/` 且成空壳。技术栈变更只改 `.md/prompts/` 一处，再同步 `.cursor/rules/ref-tech-stack.mdc` 指针段；`rules/` 始终保持仅 `project-rules.md` 一个文件。
- **Cursor 形态（统一：`.cursor/rules/*.mdc` 模块化）**：技术栈索引与版本禁区落到 `.cursor/rules/ref-tech-stack.mdc` 等 `.mdc` 文件，与 `rules/`/`skills/` 同处 `.cursor/`；**不建根 `.cursorrules` 单文件**。wlyd / bondee / yd 均统一采用此形态（历史上 bondee 曾混用根 `.cursorrules` + `.cursor/rules/*.mdc` 双形态，已统一为模块化）。校验时技术栈索引只在 `.md/prompts/stack-index.md` 与 `.cursor/rules/ref-tech-stack.mdc` 指针两处。
  - ⚠️ 两种都是 Cursor 合法形态，**同一项目内不要混用**；改动前先 `ls` 确认本项目 Cursor 实际目录形态。

### 3. 同步根文档 `README.md` 与 `AGENTS.md`

两文件**必产且须同步**，重点位置：

- **结构树**：迁移/新增父层时，结构树补出父目录（如新增 `website/` 父层，子项目挂其下）；删除时移除整棵子树。
- **子项目表**：路径单元格加前缀 / 加新行 / 删旧行，技术栈摘要对齐 `stack-index`。
- **快速开始 `cd` 命令**：`cd <proj>` 必须带完整前缀（如 `cd website/spatio-clotho-hybrid`）。
- `AGENTS.md` 的「子项目」表与「仓库结构」树同上述处理。

### 4. 检查「子项目代号」引用（保留，勿误改）

`role-*` / `snippets/` / `skills/` 里以**身份标识**出现的 `<项目代号>`（如 `spatio-clotho-hybrid`、`siren-h5`）**不是路径引用，按防漏规则可保留**，除非该处本就是路径语义（如 `cd <代号>`、`` `<代号>/src` ``）。改前先 grep 全仓区分两类：
- 路径引用 → 带完整前缀改；
- 身份代号 → 保留。

### 5. 校验清单（改完必跑，防漏的最后一道闸）

用 shell 在**文档目录**（含隐藏目录）复搜，全仓 GUI 搜索会被 gitignore 跳过隐藏目录而误判"已干净"：

```bash
cd <proj>
# ① 残留旧根级路径（应无输出）—— 以 hybrid 迁 website/ 为例
grep -rn "spatio-clotho-hybrid" README.md AGENTS.md .md .codebuddy .qoder .cursor 2>/dev/null \
  | grep -v "website/spatio-clotho-hybrid"      # 带新前缀的行剔除，剩的即漏改
# ② 残留裸 cd 命令（应无输出）
grep -rn "cd spatio-clotho-hybrid" README.md AGENTS.md .md .codebuddy .qoder .cursor 2>/dev/null \
  | grep -v "website/"
# ③ 技术栈索引只在 .md/prompts/ 一处（不进 rules/）；Cursor 落到 .cursor/rules/ref-tech-stack.mdc
#    改技术栈只改 .md/prompts/stack-index.md，再同步 .cursor/rules/ref-tech-stack.mdc 指针段
for f in .md/prompts/stack-index.md .cursor/rules/ref-tech-stack.mdc; do
  echo "$f -> $(grep -c "website/spatio-clotho-hybrid" "$f")"
done
#    （确认 .codebuddy/rules/ 与 .qoder/rules/ 下没有 stack-index.md —— rules/ 只放 project-rules.md）
```

- ①、② **无输出** = 无残留；③ `.md/prompts/stack-index.md` 与 `.cursor/rules/ref-tech-stack.mdc` 计数应一致（技术栈索引同源，rules/ 无 stack 副本）。
- 删除场景：反向校验——确认旧项目名在索引/stack/结构树/子项目表/`cd` 中已 0 出现。

### 6. 一句话速记

> **技术栈源（`.md/prompts/`）一处改 → 同步 `.cursor/rules/ref-tech-stack.mdc` 指针段 → 两文档（树/表/cd）跟着改 → shell 逐目录复搜 0 残留 → 代号保留路径改。（`rules/` 只放 `project-rules.md`，不塞 stack）**

---

## 八、Bondee 实战示例（已落地，可直接参考取值）

- 子项目 → 技术栈文件：`hybrid`/`admin` → `stack-react-umi`；`audit`/`sso-auth-page` → `stack-react-vite`；`h5` → `stack-react-webpack-ssr`；`figma-plugin` → `stack-react-figma`。
- 关键事实：全仓**无 Sass/SCSS**（Web 用 Less，Figma 用原生 CSS）；React 版本分散 17/18/19。
- 知识库：`context/` 三份（api-patterns / business-terms / known-pitfalls）**直接维护在各工具目录**（`.codebuddy/context/`、`.cursor/context/`、`.qoder/` 根），**无 `.md/context` 统一源**。（注意：Bondee 把 context 放 `.qoder/` 根，yd 不采用此做法。）
- 技能：`.codebuddy/skills/` 含 bondee-code-analyzer / bondee-commit-helper / bondee-review / bondee-workflow / bondee-requirement-change-analysis / bugfix-helper / memory-update（**bondee 约定：除 `bugfix-helper`/`memory-update` 外统一加 `bondee-` 前缀**，含 `code-analyzer`/`commit-helper`——与「通用技能不加前缀」基准规则不同，属 bondee 自身约定）；全部镜像 `.qoder/skills/` 与 `.cursor/skills/`。
- 片段：`.codebuddy/snippets/` 含 react-component / api-method / number-formatter 模板。
- 文件位置：`.md/`（源）、`.codebuddy/`、`.qoder/`、`.cursor/`（镜像）、`README.md`、`AGENTS.md`。

## 九、yd 实战示例（已落地，与 Bondee 关键差异）

- **同时使用 Sass 与 Less**：Sass 用于 `siren-h5`/`siren-website`/`siren-mgt`/`siren-mgt-front`；Less 用于其余（resource、spatio 系列、zeus）。索引须显式分列，勿跨项目混用。
- **两框架族**：React（多数）与 Vue 3（`siren-mgt` 系列）。
- **历史版本禁区**：`spatio-clotho-audit`/`spatio-clotho-h5` 的 `react-dom` 为 16.x **勿升级**；`zeus/fe` 为 React 16.8 + Umi 2 老栈。
- stack 文件：`stack-react-sass-webpack` / `stack-react-sass-next` / `stack-vue3-vite-sass` / `stack-react-less-umi` / `stack-react-less-webpack-ssr` / `stack-react-less-umi2-zeus`。
- 已落地：`.md/prompts/`（技术栈唯一源）、`rules/` 仅 `<proj>-project-rules.md`（已重建，含 alwaysApply 综合规范 + 技术栈指针）、Cursor `.cursor/rules/*.mdc` 模块化（非根 `.cursorrules` 单文件）、`README.md`、`AGENTS.md`（含四张索引表，Skills 7 项 / Snippets 4 项）。

## 十、yd 技能与知识库（已落地，真实内容）

- **技能（真实，非空壳）**：`.codebuddy/skills/` 含 7 个 `SKILL.md`（权威源），镜像 `.qoder/skills/` 与 `.cursor/skills/`：
  - `review`（审查，含 yd 版本禁区 + Sass/Less 分用）、`workflow`（全链路流程编排）、`commit-helper`、`code-analyzer`、`requirement-change-analysis`、`bugfix-helper`、`memory-update`。
  - 技能命名（以 yd 实际文件为准）：全部 7 项**均不加 `yd-` 前缀**——`bugfix-helper` / `code-analyzer` / `commit-helper` / `memory-update` / `review` / `workflow` / `requirement-change-analysis`（含基准规则中列为专属的 `review`/`workflow`/`requirement-change-analysis`，yd 也未加前缀，属 yd 自身约定）。此前曾误删 `yd-project-rules.md`、并误判技能命名，现已按实际文件校正。
  - yd 专属约束用已证实事实：react-dom 16.x / Umi2 禁升级、Sass 与 Less 按子项目分用勿混、SSR 顶层禁 `window`、Vue3 用 vue-i18n / React 用 i18next。项目特有 util 以实际代码为准，不脑补 CDN 域名等。
- **知识库**：yd 当前技术栈事实（框架/样式/版本禁区/请求封装/i18n）已全部并入 `prompts/stack-*`，**不单独建 `context/*.md`**（避免重复）；如需沉淀真实业务术语/坑点，直接写到各工具目录 `.codebuddy/context/` 与 `.cursor/context/`，勿建 `.md/context` 源。
- **片段**：`.codebuddy/snippets/` 4 份（React/Vue 组件、API 方法、数字格式化）。
- **Cursor 配置**：`.cursor/settings.json` + `extensions.json`（React+Vue 双栈）。
- **角色 prompt（`role-*`，11 个）**：`.md/prompts/` 已建齐（与 Bondee 对齐）：`role-pm` / `role-architect` / `role-coder` / `role-reviewer` / `role-qa-checklist` / `role-bugdetective` / `role-handoff` / `role-refactor` / `role-resume` / `role-ui-reviewer` / `role-workflow`。骨架照 Bondee，强制规范换成 yd 真事实；去掉 Bondee 专属的 gslb CDN / NumberFormatter / iOS14 禁 gap / localStorage 隔离，项目特有 util 以实际代码为准。`role-workflow.md` 与 `workflow` Skill 互为等价（手动 `@role-*` 多 Chat 场景用）。

---

## 十一、规定来由（历史踩坑清单，仅作背景，可不读也能做对）

> 以下 11 条来自 yd / bondee 项目实际搭建中 AI 犯过、被人工纠正的错误。**正文第一～十章已将其正确做法固化为正向约束**，本附录只解释"为什么这么规定"。新项目直接照第五章流程即可，不必从这里学。

### 来由 1：Cursor 规则误放成 `.cursor/rules/` 文件夹
早期在 `yd/.cursor/rules/` 下建了一堆 `.mdc`（还留了 0 字节的 `yd-project-rules.mdc`）。正确形态是 **yd 统一采用 `.cursor/rules/*.mdc` 模块化**（规则位于与 `AGENTS.md`/`README.md` 同级目录内的 `.cursor/rules/`），`.cursor/` 同时承载 rules/skills/snippets/context。Bondee 早期混用根 `.cursorrules` + `.cursor/rules/*.mdc` 双形态，现已与 yd / wlyd 统一为模块化形态——同一项目不要混用两种形态（不要同时保留根 `.cursorrules` 与 `.cursor/rules/*.mdc`）。

### 来由 2：照搬参考项目的专属内容（脑补）
写 yd 的 rules/role/skill 时，直接抄了 bondee 专属的 `gslb.bondee.net` CDN、`NumberFormatter`、`getLocalTextViaUniversalLangKeyFromStorage`、iOS14 禁 `gap`、localStorage 账号隔离。正确做法是**项目特有工具一律以实际代码为准**，通用骨架可复用，特有事实必须换成当前项目真值。

### 来由 3：只建 stack 文件，漏掉 `role-*` 角色 prompt
`yd/.md/prompts/` 只生成了 `stack-index` + `stack-*` 共 7 份，缺的是 **11 个 `role-*` 角色 prompt**。正确做法是角色 prompt 与技术栈文档**同批产出**；角色 prompt 不必镜像进 `rules/`，只在 `.md/prompts/` 源里。

### 来由 4：把 `stack-*` 镜像塞进 `rules/` + 误删 `project-rules.md`
早期错误地把 `stack-index` / `stack-*` 当成"副本"同步进 `.codebuddy/rules/`、`.qoder/rules/`，并把 `yd-project-rules.md`（`alwaysApply` 项目规则单文件）误判为"重复/无效"而删除。按本手册标准，`rules/` 只放**一个** `<proj>-project-rules.md`（综合开发规范 + 技术栈索引指针），`stack-*` 只存在于 `.md/prompts/` 单源、绝不进 `rules/`。正确做法：**`rules/` 只保留 `<proj>-project-rules.md` 一个文件**，技术栈只在 `.md/prompts/` 一处维护；`project-rules.md` 是必产项（带 `alwaysApply: true`），不可当作冗余删掉。另：删文件前先确认是否被引用，不要替用户拍板删文件。

### 来由 5：知识库 `.md/context/` 整层是冗余发明
看到 bondee 各工具目录都有 `context/`，就脑补出 `.md/context/`「唯一可编辑源」去镜像——但参考项目里根本没有这一层。为填满它还把 stack 已有事实抄了一遍、`business-terms.md` 留成 0 字节空文件。正确做法是**知识库无 `.md/context` 统一源**，直接写各工具目录、内容不重复 stack、不留空壳。

### 来由 6：skills 内残留过时路径 / 引用名对不上
`memory-update` skill 的同步步骤应写「技术栈同步到 `.cursor/rules/ref-tech-stack.mdc`」。正确做法是搭建/改动 skills 时**通读每个 SKILL.md 的路径与引用名**，与实际目录对齐、一并同步三工具副本。

### 来由 7：漏建 `.cursor/skills/` 与 `.cursor/snippets/`
曾误以为 Cursor 无独立 skills 目录，导致 `.cursor/` 只有 `context/` 没 `skills/`。`.cursor/` 明确有 `skills/` 与 `snippets/`——Cursor 也有独立目录，须三处（skills）/两处（snippets）镜像（见第二章目录结构）。

### 来由 8：`README.md` 与 `AGENTS.md` 生成质量差
模板原本漏写 `AGENTS.md` 规范、`README.md` 只有空表。正确做法是**两份必产且达参考项目信息密度**：README 含结构树+子项目技术栈表+环境要求+配置维护+相关链接；AGENTS 含四张索引表。

### 来由 9：只更新本模板、却没同步落地到实际项目文件
在模板里修好 `README`/`AGENTS` 规范却只改了模板文件，没改 `yd/` 对应文件，用户发现"还是没更新"。正确做法是**模板是规范不是产出**，改动须同轮落地在维护项目（yd）对应文件并确认。

### 来由 10：路径迁移只改「归属描述行」，漏掉文件内 `cd` 命令与副本，且被 gitignore 跳过隐藏目录误判"已干净"
`yd/` 下 siren 系列 5 个子项目整体从根迁移到 `siren/` 子目录。初次替换只改了各 `stack-*.md` 顶部的「项目归属描述行」和子项目表，却漏了同文件内后续的 `cd siren-h5` / `cd siren-mgt` 等**可执行启动命令**，以及 `AGENTS.md` 快速开始的 `cd siren-mgt`；同时一次全仓搜索"看似无残留"，实则 `.codebuddy/`、`.qoder/`、`.cursor/`（含 `.cursor/rules/`）被 gitignore 跳过未被搜到，副本里同样的 `cd` 命令也全是旧的。正确做法见第六章「路径/重命名变更防漏检查清单」：**按类型全量替换 + 逐镜像目录分别复搜**，路径引用带完整前缀、代号保留。

### 来由 11：缺少"子项目增删 / 目录调整"的统一步骤，每次都临时摸排、易漏镜像
bondee `website/` 调整（hybrid 从根迁入 `website/` + 新增 `website-toc` / `website-toc-h5`）时，先改唯一源，再同步三处镜像，才发现 `.qoder/`、`.cursor/` 的 `stack-*.md` 早就是**空壳（0 行）**——即历史镜像从未真正写入。最终用已正确的 `.codebuddy/rules/` 副本 `cp` 整体补齐（bondee 形态下 `.cursor` 用 `.mdc` 并加 frontmatter）。据此把"增删/迁移"抽象为可复用 SOP（第七章）：**源一处改 → 三镜像同步（先查空壳）→ 两文档树/表/cd 跟着改 → shell 逐目录复搜 0 残留 → 代号保留路径改**，使后续同类调整可快速接入、不漏不重。

---

## 十二、种子脚手架与换设备快速接收

> 本手册为**自包含标准**，不依赖任何外部项目目录（wlyd / bondee / yd 仅作可选本地示例，非必需）。**本手册所在的 `ai-tooling-seed/` 目录即配套的种子脚手架**，用于换设备后 / 新项目快速补齐 AI 工具配置。

### 1. 种子脚手架是什么
`ai-tooling-seed/`（**本手册所在目录**）是**可复制初始骨架**，内含：
- `.md/prompts/`：技术栈 + 11 个角色 prompt 模板（填内容用）
- `templates/`：项目级骨架（`AGENTS.md` / `PROJECT-README.md`），含 `<proj>` 占位；Cursor 模块化规则在 `.cursor/rules/*.mdc`（随 `.cursor/` 整体复制）
- `.codebuddy/`：权威源（`rules/project-rules.template.md` + 7 个通用技能 + 4 个片段（react/vue 组件、api、number 格式化，按框架增删））
- `.qoder/`：rules + skills 镜像位置
- `.cursor/`：skills + snippets 镜像 + `settings.json` / `extensions.json` + `context/` 占位

### 2. 换设备快速接收项目（六步）
1. **放骨架**：把 `ai-tooling-seed/`（含本手册）整体复制到目标项目根（或 `git clone` 后重命名）。
2. **改名规则**：`project-rules.template.md` → `<proj>-project-rules.md`（`.codebuddy/` 与 `.qoder/` 两处）。
3. **镜像技能/片段**（`.codebuddy/` 为权威源）：
   ```bash
   mkdir -p .qoder/skills .cursor/skills .cursor/snippets
   cp -r .codebuddy/skills/* .qoder/skills/
   cp -r .codebuddy/skills/* .cursor/skills/
   cp -r .codebuddy/snippets/* .cursor/snippets/
   ```
4. **填技术栈**（最关键，禁脑补）：编辑 `.md/prompts/stack-index.md` 与各 `stack-<type>.md`，先读各子项目 `package.json` 与 `*.less/*.scss/*.css` 核实。
5. **填专属事实**：`role-*.md`、各 `SKILL.md` 的 `<proj>` 占位与版本禁区、CDN 等以实际代码为准。
6. **落地文档**：`templates/AGENTS.md` / `templates/PROJECT-README.md` → 项目根 `AGENTS.md` / `README.md`；`.cursor/rules/ref-tech-stack.mdc` 技术栈索引指针（与 `.md/prompts/stack-index.md` 对齐）；校验按第七章。

### 3. 与已有项目的关系
- 标准以本手册为准；`wlyd/` 等若存在本地，仅作对照示例。
- 技能命名：种子默认 7 个通用技能**不加前缀**；若项目要专属化，按本手册基准（通用 4 项不加、专属项加 `<proj>-` 前缀）自行取舍。

---

**文档版本**: v7.9
**最后更新**: 2026年07月
**整理人**: 王新骏
