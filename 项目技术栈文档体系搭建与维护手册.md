# AI 工具配置搭建与维护手册（v2）

## 一、核心原则

1. `.cursor/` 是 rules、prompts、skills、snippets、context 和编辑器配置的唯一人工维护源。
2. `.codebuddy/`、`.qoder/`、`.md/prompts/` 是生成物，不直接编辑。
3. 技术栈事实必须来自 `package.json`、锁文件、构建配置、真实源码或用户确认。
4. 初始化必须先 dry-run；已有文件自动备份，无法可靠合并的文件不覆盖。
5. 单仓库和 monorepo 都是正常形态，不为单仓库虚构子项目。
6. 通用核心不包含称呼探针、金额工具、CDN、旧代码注释保留等项目专属策略。

## 二、目录

```text
.cursor/                 唯一源
├── ai-tooling.json      探测结果与启用策略
├── rules/               Cursor 模块化规则
├── prompts/             stack 与角色 Prompt
├── skills/
├── snippets/
└── context/

.codebuddy/              生成物
.qoder/                  生成物
.md/prompts/             兼容生成物
```

## 三、新项目接入

```bash
python3 scripts/ai_tooling.py detect /path/to/project
python3 scripts/ai_tooling.py init /path/to/project --dry-run
python3 scripts/ai_tooling.py init /path/to/project
python3 scripts/ai_tooling.py verify /path/to/project
```

检查 `detect` 输出中的包路径、框架、构建工具、样式和证据。自动探测未覆盖的业务事实写入项目 Context 或 stack 文档，不修改生成目录。

## 四、日常维护

```bash
# 修改 .cursor 后刷新
python3 scripts/ai_tooling.py generate .

# 提交前校验
python3 scripts/ai_tooling.py verify .
```

`generate` 根据 Cursor `core-*.mdc` 生成 CodeBuddy/Qoder 单文件规则，并镜像 Skills、Snippets、Context 和 Prompts。输出是确定性的，重复执行不会产生差异。

## 五、自动合并与恢复

- README/AGENTS 使用 `ai-tooling:start/end` 标记维护区块。
- JSON 中已有值优先，脚手架只补缺失键。
- 其他 Cursor 源文件遇到同名内容时保留目标项目版本。
- 所有被修改的已有文件先备份到 `.ai-tooling/backups/`。
- 发生冲突时查看 CLI 的 `CONFLICT` 行，人工处理后重新运行。

## 六、Profile

- `frontend`：默认，包含前端规则、角色、Skills 与 React/Vue 示例片段。
- `generic`：保留语言无关的协作能力；后端框架专项规则留待真实项目出现后增加。

## 七、校验范围

校验器检查 manifest、JSON、规则 frontmatter、生成物漂移、镜像完整性、占位符、非法跨平台文件名、坏链接、旧项目残留、受控区块数量和疑似凭据 URL。预期失败只输出结构化问题，不显示 Python traceback。

旧版实践、历史事故和项目专属案例保存在 `docs/legacy/wlyd-derived-practices.md`，不再进入新项目默认配置。
